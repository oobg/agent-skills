# 저장소 개인정보 지침

- 공개 저장소에는 재사용 가능한 스킬 지침·코드·합성 예시만 둔다.
- 실제 이력서·연락처·개인 경력·회사/고객 기밀·원문 대화·평가 입력·로그는 저장소 밖 로컬에 보관한다.
- 예시는 실자료의 단순 이름 치환으로 만들지 말고, 추적 가능한 고유명사와 수치를 제거한 합성 데이터로 작성한다. 경로 예시는 `~/...` 또는 `<placeholder>`처럼 일반화한다.
- 기존 `/evals/` ignore를 유지하고 강제 add하지 않는다.
- 커밋 전 staged diff와 새 파일에 개인 원문이나 식별정보가 없는지 확인한다. 발견하면 값은 출력하지 말고 경로와 종류만 기록한 뒤 제거하거나 합성 예시로 대체한다.
- 이 지침은 다른 사용자의 변경 삭제, 이력 재작성, push 승인을 뜻하지 않는다.
